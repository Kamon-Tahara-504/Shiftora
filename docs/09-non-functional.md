# 09. 非機能要件

## 共通

- Docker による環境統一
- Railway でのデプロイ
- RLS の本番導入
- **監査ログ:** `audit_logs` にイベントを記録。**削除しない**。ログは基本的に「残すほうが安全」。
- エラーハンドリングの統一
- OpenAPI 自動生成

---

## ログイン試行・監査

- **ログイン試行制限（ロックアウト）は MVP では不要。** 将来の TODO として残す。
- **監査ログは削除しない。**

---

## タイムゾーン

- **DB は UTC 保存。** 理由: PostgreSQL の標準、将来多地域対応できる、バックエンドが安定。
- **フロントで JST 表示に変換する。**

---

## 祝日・休業日

- **MVP では入れない。** 施設ごとに違うため。後から追加する。必要なら `facility_closures` テーブルを追加すればよい。

---

## 希望休の締切

- **MVP では不要。** API は単純に受け付ける。締切管理は運用ルールで対応できる。
